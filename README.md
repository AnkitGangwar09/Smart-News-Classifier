# Smart-News-Classifier
An End to End production ready machine learning project which in very simple terms will classify news into their category.

To run webscrapper file, go to directory where project folder is , in terminal, write
>> scrapy crawl spider_name -O file_name.csv
our spider name was "newser" and we saved file in csv format named "newser.csv"
